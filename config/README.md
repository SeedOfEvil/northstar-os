# Configuration

This tree will hold Northstar's Wayfire, session, D-Bus, display-manager, and default configuration overlays. Configuration must be explicit, documented, and safe to apply idempotently. It must not silently replace unrelated administrator settings.
