# Opt-in SDDM configuration for the Proxmox basic-VGA compatibility lane.
# Native hardware should use the normal display-server policy instead.

[General]
DisplayServer=x11

[Theme]
Current=northstar
