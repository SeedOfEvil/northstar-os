# Image assembly

Image work is deferred until package and session behavior is stable. The first artifact target is a reproducible ZFS QCOW2 image for QEMU/Proxmox, followed by raw and USB images. ISO assembly is later work.

Generated images, release sets, package repositories, and signing material are ignored by Git.
