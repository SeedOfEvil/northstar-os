# Northstar M4 sprint validation — 2026-08-09

This note records the current evidence for the separate FreeBSD validation
checkout. It does not replace the interactive noVNC acceptance note and does
not claim direct DRM/KMS or native multi-display support.

## Source and VM

- Repository: `https://github.com/SeedOfEvil/northstar-os.git`
- Integration branch: `codex/m4-sprint-integration`
- Integration head: `49ea26a` (`Docs: record current M4 sprint validation evidence`)
- FreeBSD VM: `northstar@192.168.1.215`
- Validation checkout: `/home/northstar/src/northstar-files-v3-ab1c56a`
- Install prefix: `/home/northstar/.local`
- Canonical checkout `/home/northstar/src/northstar` was not modified.
- Graphics lane: Proxmox basic-VGA/scfb nested X11/pixman; direct DRM/KMS,
  native multi-display, and physical Intel/AMD evidence remain open.

## Product slices promoted into integration

| PR | Squash commit | Slice | Automated state |
| --- | --- | --- | --- |
| [#56](https://github.com/SeedOfEvil/northstar-os/pull/56) | `069b535` | Software Center application catalog | Passed |
| [#57](https://github.com/SeedOfEvil/northstar-os/pull/57) | `3a55fcd` | Read-only update-plan review | Passed |
| [#58](https://github.com/SeedOfEvil/northstar-os/pull/58) | `b5abf4e` | Files tile/list preference persistence | Passed |
| [#59](https://github.com/SeedOfEvil/northstar-os/pull/59) | `148c068` | Supervised shell restart action | Passed |

The consolidated integration PR [#51](https://github.com/SeedOfEvil/northstar-os/pull/51)
remains the promotion boundary to `main` until the manual console gate is
observed.

## Automated evidence

Run from the validation checkout after syncing the exact source archive:

```sh
cmake --build build
env QT_QPA_PLATFORM=offscreen ctest --test-dir build --output-on-failure
make qml-surface-test
cmake --install build --prefix /home/northstar/.local
sh tests/unit/test-session-script.sh
sh tests/unit/test-session-entrypoint.sh build
env QT_QPA_PLATFORM=offscreen /home/northstar/.local/share/northstar/apps/NorthstarWelcome.app/Contents/Executable/northstar-welcome-gui --self-test
env QT_QPA_PLATFORM=offscreen /home/northstar/.local/share/northstar/apps/NorthstarTextEditor.app/Contents/Executable/northstar-text-editor --self-test
```

Results for this integration head:

- Native build: passed for the complete configured target graph (114 Ninja
  steps in the fresh validation checkout).
- Qt regression suite: **17/17 passed**.
- QML surface contract: passed.
- Session supervisor scenarios: passed, including bounded restart, duplicate
  session rejection, and owned-child cleanup.
- Session entry-point staging: passed.
- Welcome and Text Editor self-tests: passed.
- Installed shell offscreen smoke: passed with only the expected
  `layershellqt ... is not a wayland window` notices; no QML reference,
  binding, syntax, or runtime errors were reported.
- Live shell smoke: **OPEN**. Both `make shell-smoke` and
  `make shell-restart-smoke` reached their checks and stopped at the expected
  `FAIL: WAYLAND_DISPLAY is not set` precondition because the VM was at the
  SDDM greeter with no active Northstar user session.
- Graphics observation: `/dev/dri` is absent on this Proxmox basic-VGA/scfb
  lane; this remains supplemental nested X11/pixman evidence only.

## Open manual evidence

The VM was at the SDDM greeter with no active Northstar Wayland session during
the SSH validation. Therefore these remain `OPEN`, not passed by inference:

1. Start the supported Northstar session through the Proxmox/noVNC console.
2. Switch Files between Tiles and List, restart only the Northstar shell, and
   confirm the chosen view returns.
3. Open Settings > Session, confirm **Restart Northstar Shell**, and verify the
   shell is replaced while the compositor and unrelated applications survive.
4. Repeat the Desktop, Files, association, Trash, Apps, Software Center, Dock,
   Settings, and graceful reboot workflows from the consolidated checklist.

The current noVNC/browser session was not available to capture those
observations. The live `make shell-smoke` check therefore remains an
environment gate, not a failure of the headless slice. Direct DRM/KMS and
native multi-display acceptance remain later hardware gates even after noVNC
passes.
