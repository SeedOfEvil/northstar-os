# M7 Files v3 validation — 2026-08-17

PR #94 was validated from `codex/m7-files-v3` on the supplemental
NSTAR-DEV01 FreeBSD 15.1 Proxmox/noVNC lane at 1280×800.

## Automated evidence

- Product candidate `59a7f39225396fefa7fa2fc27c4cbe6c853f4fcd` was exported as
  an immutable archive with SHA-256
  `2fb93c5c94232d6758798e237ab01142281f9bbc9cc3994a492c16e70c08e92c`.
- All shell, installer, image, session, and QML contract scripts passed.
- The clean FreeBSD build completed all 363 targets.
- Twenty-eight unchanged native suites passed in the complete 29-suite run.
  The remaining Files suite exposed only an undersized two-second asynchronous
  test deadline; after widening that deadline, the exact final Files/shell
  candidate rebuilt all 132 focused targets and passed all 23 Files cases.
- `git diff --check` passed.

## Deployment evidence

- The pushed branch head was
  `4ada8bb114307e62c7b992eec9d1f9439c464846` (the product candidate plus a
  documentation-only PR-number correction).
- The prior dirty M5 checkout was preserved intact at
  `/home/northstar/quarantine/20260817-pre-pr94-4ada8bb` rather than reset or
  deleted.
- `/home/northstar/src/northstar` was cloned cleanly from the pushed branch and
  matched the remote head.
- The canonical build completed at
  `/home/northstar/builds/pr94-4ada8bb` and installed below
  `/home/northstar/.local`.
- DEV01 was left at the SDDM greeter; login used
  **Northstar (Proxmox X11 fallback)** so the new installed shell loaded from a
  clean session.

## Interactive acceptance

The user completed the focused noVNC checklist and reported the slice working.
The accepted surface covers independent Files tabs, Ctrl+L home navigation,
responsive asynchronous copy/move, explicit Keep Both conflict handling,
recoverable one-step undo, read-only mounted-volume mutation boundaries, and
unclipped controls in the 1280×800 VM lane.

This acceptance closes the Files v3 daily-driver slice. It does not change the
separate M6 physical Intel/AMD DRM, multi-display, or native compositor gates.
