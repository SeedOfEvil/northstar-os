# Contributor tools

Planned stable wrappers are `check-host.sh`, `bootstrap-dev.sh`, `run-session.sh`, and `collect-diagnostics.sh`. They will be added with M0 implementation work and must be idempotent where applicable, avoid secrets, and document manual recovery.
