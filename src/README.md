# Northstar source

The source tree is reserved for common libraries, the shell, session, launcher, settings, notifications, and search services. PR 1 defines the boundaries but intentionally does not add implementation components.
