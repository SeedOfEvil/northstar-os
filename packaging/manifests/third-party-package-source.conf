# Root-owned trust policy for Software Center third-party package actions.
# The transaction runner compares every value with pkg's active configuration
# and binds each plan to the refreshed catalogue digest.
protocol=1
repository=FreeBSD-ports
abi=FreeBSD:15:amd64
ports_branch=2026Q3
url=pkg+https://pkg.FreeBSD.org/FreeBSD:15:amd64/quarterly
mirror_type=SRV
signature_type=FINGERPRINTS
fingerprints=/usr/share/keys/pkg
